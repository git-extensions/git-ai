name: test
on:
  push:
    branches: ["**"]
  pull_request:
    branches: [main]
jobs:
  unit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: bats-core/bats-action@v4
      - name: Run tests
        run: bats tests/
